This package contains compatibility tests with the Go standard library, and some
experiments with e.g. bytes and sizes. You shouldn't want or need to import this
package into your Javascript program.
